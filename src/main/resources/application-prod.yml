spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/testedb?useTimezone=true&serverTimezone=UTC
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: postgres
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    properties:
      hibernate:
        show_sql: false
        format_sql: false